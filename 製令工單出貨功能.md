# 製令工單出貨功能說明

本文件描述系統中的 **製令領料／出庫（介面為「製令工單出庫 MO Pick」）** 之行為：資料來源、操作步驟、後端規則、與紀錄／報表的關聯。

---

## 1. 功能定位與進入方式

| 項目 | 說明 |
|------|------|
| 選單路徑 | **出入庫作業** → 分頁 **製令工單出庫 (MO Pick)** |
| 目的 | 依 **製令編號** 載入領料清單，掃描儲位與數量完成扣帳出庫，並更新製令 **已領用量**；亦可選 **略過不扣帳** 完成結案。 |
| 與「主件／BOM 出庫」差異 | 製令資料來自 **製令工單匯入**（`work_order_lines`），非主件配方 BOM。 |

---

## 2. 資料來源與前置作業

1. **匯入製令**  
   - 於 **資料匯入中心** 依範本匯入製令工單清單。  
   - 同一 **製令編號** 再次匯入會 **刪除該製令舊明細** 後以新檔覆寫。  

2. **總表儲存**  
   - 明細表：`work_order_lines`（製令編號、開單日、材料品號、品名、需領用量、已領用量等）。  

3. **庫存查詢／庫存報表**  
   - **庫存查詢 → 製令查詢**、**庫存報表 → 製令工單總表** 皆讀取 `work_order_lines` 彙總後顯示。  
   - 當製令 **全數領畢且符合結案規則** 後（見 §6），該製令會從上述總表 **移除**（見 §6）。

---

## 3. 載入製令（輸入製令編號）

- 使用者在製令編號欄輸入後，前端呼叫 **`GET /api/work-orders/for-out?work_order_no=...`**。  
- **成功**：回傳製令編號、開單日期、各材料行（含資料庫已領、即時庫存／儲位字串等，供領料參考）。  
- **失敗（404）** 常見原因：  
  - 資料庫無此製令；  
  - 尚未匯入；  
  - 該製令 **已全部領畢並已自總表刪除**（無 `work_order_lines`）。  

若查無資料，畫面提示可至 **製令工單出入庫紀錄** 查歷史；若要再行領料需 **重新匯入製令**。

---

## 4. 操作畫面流程

### 4.1 開始領料

- 製令資料載入成功後，使用者按 **開始領料**。  
- 系統將本次作業暫存在瀏覽器（session），包含：`staged_picks`（暫存扣帳掃描）、`skipped_barcodes`（略過不扣帳料號）。

### 4.2 暫存出庫掃描

- **條碼**：須為該製令清單內的材料品號。  
- **儲位**：須為實際扣帳儲位；系統會比對該料建議／實際儲位字串，不符則阻擋。  
- **數量**：不可超過「剩餘應領」（需領 − 資料庫已領 − 本次暫存累計）。  
- 每筆暫存會增加右側 **本次暫存已領**，結案時一併送後端。

### 4.3 略過不扣帳

- 每列材料可選 **略過(不扣帳)**。  
- 意義：該料 **不扣即時庫存**，但結案時仍會依 **剩餘應領** 寫入一筆 **略過** 出庫紀錄，並 **增加已領用量**（與實際扣庫分開）。  
- **同一料號** 不可同時出現在「暫存掃描」與「略過」；若該料已有暫存，須先結案或取消作業再略過。  
- 已略過的料號 **不可再暫存掃描** 同一批作業。

### 4.4 確認領料出庫（結案）

- 可 **部分結案**（尚有未領滿亦確認，依實際暫存＋略過處理）。  
- 若 **完全沒有暫存掃描且沒有略過**，會再詢問是否關閉（不送後端則資料不變）。  
- 結案呼叫 **`POST /api/transactions/mo-out`**。

---

## 5. 後端 `POST /api/transactions/mo-out`

### 5.1 請求 Body（摘要）

```json
{
  "work_order_no": "製令編號",
  "staged_picks": [
    { "barcode": "材料品號", "location_code": "儲位代碼", "quantity": 數值 }
  ],
  "skipped_barcodes": ["略過之品號", "..."]
}
```

- `staged_picks` 與 `skipped_barcodes` **至少一個要有內容**（可其一為空陣列）。  
- **同一製令、同一請求** 內同一料號不可同時出現在掃描與略過。

### 5.2 一般掃描扣帳

- `ref_order`：`製令:{製令編號}`  
- `mo_skip`：`0`  
- 依儲位 **扣減 `inventory`**，並寫入 **`transactions`（OUT）**。  
- 對應 `work_order_lines` 之 **已領用量** 依本次數量增加。

### 5.3 略過不扣帳

- 寫入 **`transactions`（OUT）**，`mo_skip = 1`。  
- `ref_order`：`製令:{製令編號}|略過`  
- `location_id`：系統內建儲位 **`SYSTEM-MO-SKIP`**（僅供紀錄邏輯用，不影響實體倉扣帳）。  
- **不異動 `inventory`**；仍增加該行 **已領用量**（略過量＝當次剩餘應領扣掉同料號暫存合計後之餘額）。

### 5.4 回應（摘要）

- `processedPickLines`：成功處理的暫存筆數。  
- `processedSkips`：成功處理的略過筆數。  
- `work_order_fully_picked`：本次更新後，該製令是否 **所有明細已領滿**（在誤差容許內）。

---

## 6. 全數領畢後與總表、紀錄的關係

- 當 **`work_order_fully_picked` 為 true** 時，後端於 **同一交易** 內會 **刪除該製令所有 `work_order_lines`**。  
- 效果：  
  - **庫存查詢（製令）**、**庫存報表｜製令工單總表** 不再顯示該製令。  
  - **出入庫／製令工單出入庫紀錄** 中的 **`transactions` 仍保留**（含一般扣帳與略過列），供稽核與報表。  
- **再次領同一製令**：須 **重新匯入** 製令清單。  

**匯入製令** 時，若檔案中該製令每一列 **已領用量已等於需領用量**，匯入後亦會 **自動刪除該製令明細**（不留在總表）。

---

## 7. 製令工單出入庫紀錄與作廢（主管）

- **出入庫紀錄** 可分頁檢視 **一般出入庫** 與 **製令工單出入庫**（`kind=mo` 僅顯示 `ref_order` 以 `製令:` 開頭者）。  
- **略過** 列可從 `mo_skip` 或儲位／備註辨識。  
- 主管 **作廢** 製令出庫紀錄時：  
  - **一般扣帳列**：**還原庫存**（同一般出庫作廢）並 **調降該製令已領用量**（若該製令明細仍存在）。  
  - **略過列**：**不還原庫存**（原未扣帳），仍 **調降已領用量**（若明細仍存在）。  
- 若該製令已因全領畢 **自總表刪除**，作廢時可能 **無對應 `work_order_lines` 可調**，系統仍會處理庫存還原（略過列除外）與交易軟刪除，但 **已領用量無法回寫**（因列已不存在）。

---

## 8. 相關 API 一覽

| 方法 | 路徑 | 用途 |
|------|------|------|
| GET | `/api/work-orders/for-out` | 製令出庫前載入明細與庫存參考 |
| POST | `/api/transactions/mo-out` | 製令領料結案（扣帳／略過／更新已領） |
| GET | `/api/work-orders` | 庫存查詢－製令列表 |
| GET | `/api/reports/work-orders` | 庫存報表－製令工單總表來源 |
| POST | `/api/admin/import/work-orders` | 管理員匯入／覆寫製令 |
| GET | `/api/transactions?kind=mo` | 製令相關交易列表 |
| POST | `/api/admin/transactions/:id/void` | 作廢單筆交易（含製令邏輯） |

---

## 9. 名詞對照（與畫面／欄位）

| 畫面或慣用語 | 說明 |
|--------------|------|
| 需領(AK) | `required_qty` |
| 已領(AL) | `picked_qty`（資料庫累計，含本次結案寫入） |
| 剩餘 | 需領 − 已領（含本次暫存時會顯示暫存後進度） |
| 製令編號(A) | `work_order_no` |

---

## 附錄：與其他功能的影響評估（詳細檢查）

以下依 **共用資料表／API** 逐項說明：**製令工單出庫**（`mo-out`）與現有功能是否衝突或可預期共用影響。

### A. 結論摘要

| 判斷 | 說明 |
|------|------|
| **不會改壞既有路徑** | 一般入出庫、`bom-out`、BOM／料件查詢、庫存報表（料件／儲位／主件／低於安全庫存）、儲位地圖資料、盤點紀錄等，**未與製令邏輯綁死**；製令專用程式走獨立 API 與 `ref_order` 命名空間。 |
| **會共用且屬合理影響** | **即時庫存 `inventory`**：製令「有扣帳」的出庫與 **一般出庫、BOM 出庫** 一樣會減少庫存——多管道共用同一批庫存是正確行為。 |
| **需知情的邊界行為** | 製令 **全領畢** 後會 **刪除 `work_order_lines`**，導致後續「作廢舊製令交易」時 **可能無法調回已領用量**（見 §7）；另 **`ref_order` 字首規則** 若被手動誤用會影響出入庫紀錄分頁分類（見下節）。 |

### B. 入出庫紀錄（`GET /api/transactions`）

- **一般分頁**（`kind=standard`）：排除 `ref_order LIKE '製令:%'`。  
- **製令分頁**（`kind=mo`）：僅顯示 `ref_order` 以 **`製令:`** 開頭者。  
- **影響**：BOM 出庫的 `ref_order` 為 `BOM:...` 形式，**永遠落在一般分頁**，與製令分頁互不搶資料。  
- **風險（低）**：若未來有人透過 **`POST /api/transactions`（單筆）** 自行填入以 `製令:` 開頭的 `ref_order` 但未走 `mo-out`，該列會出現在「製令工單出入庫」分頁，且 **作廢邏輯會依製令規則** 試圖調 `work_order_lines`——若無對應明細則略過調帳。**建議**：單筆交易勿手動偽造 `製令:` 備註。

### C. 資料庫欄位 `transactions.mo_skip`

- 由 migration **預設 0**；舊的一般／BOM 交易列皆為 **0**。  
- **影響**：僅 **製令略過不扣帳** 寫入 **1**；其餘功能讀取列表時若未使用該欄，行為不變。作廢時依 `mo_skip` 決定是否還原庫存。

### D. 儲位 `SYSTEM-MO-SKIP`

- 略過列的 `location_id` 指向此內部儲位；**略過流程不寫入 `inventory`**，故通常 **不會** 在此儲位累積實體庫存。  
- **儲位列表／地圖**（`GET /api/locations`）會多一筆系統用儲位列；若畫面未過濾「系統」樓層，使用者可能看到 **「系統」樓** 上該代碼——屬顯示層面，不影響扣帳正確性。

### E. 主件／BOM 出庫（`POST /api/transactions/bom-out`）

- 使用 **不同** `ref_order` 格式，且 **不寫** `mo_skip`。  
- **影響**：與製令出庫 **無程式路徑共用**；同一料件可能被 **BOM 與製令分別扣庫**，合計仍以 `inventory` 為準（與兩種出庫並存時相同）。

### F. 一般入庫／出庫、無貼紙入庫

- 寫入 `transactions` 時 **不含** `mo_skip`（由 DB 預設 0），`ref_order` 多為空白或自定義非 `製令:`。  
- **影響**：**無**；庫存量仍為所有出庫（含製令扣帳）一併加減後的結果。

### G. 庫存查詢、庫存報表（料件／儲位／主件／低庫存）

- 資料來自 **`inventory` + `items` + …**，**不依賴** `work_order_lines`。  
- **影響**：製令扣帳會 **改變** 這些畫面上的數字（與任何出庫相同），**不算副作用**。

### H. 庫存查詢「製令」、報表「製令工單總表」

- **直接相依** `work_order_lines`。  
- **影響**：全領畢或「匯入即全領畢」刪表明細後，**該製令自畫面消失**——為現行規格；**不依賴**交易表是否存在。

### I. 匯入製令（`POST /api/admin/import/work-orders`）

- 同一製令號 **覆寫** 會先刪後插；並在全領畢時再刪。  
- **影響**：與製令出庫 **共用同一張總表**；若線上尚在領料、管理員又用檔案覆寫同一製令，會 **重置** 明細——屬資料管理風險，非 mo-out 獨有。

### J. 刪除料件（管理功能）

- 刪除料件時會刪除相關 `transactions`。  
- **影響**：含 **製令相關** 交易一併消失；與刪除其他料件之交易行為一致。

### K. 盤點 onsite 紀錄（`stock_check_records`）

- **程式碼未** 關聯 `work_order_lines` 或製令 `ref_order`。  
- **影響**：**無**。

### L. 權限與選單

- 製令出庫在 **出入庫作業**；若角色無法進入該頁，與其他模式相同由前端路由／權限控制。  
- **影響**：僅擴充同一頁多一種 `mode`，不修改其他頁面路由邏輯。

---

*文件依目前程式行為整理；若日後 API 或欄位調整，請同步更新本檔。*
