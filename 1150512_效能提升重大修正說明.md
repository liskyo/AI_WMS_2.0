# 1150512｜效能提升重大修正說明

本文檔整理本次針對「後端查詢、傳輸負載、前端彙總運算」所做的效能優化與相容性注意事項，供維運與二次開發參考。

---

## 一、總覽

| 面向 | 作法摘要 |
|------|-----------|
| 資料庫 | 新增常用查詢欄位之索引；移除重複的 DDL 執行 |
| `/api/items` | 支援 `summary=1`，看板輪詢可略過長字串 `GROUP_CONCAT(locations)` |
| `/api/transactions` | 改為伺服端分頁、`q` 關鍵字篩選；回應為 `{ items, total, limit, offset }` |
| `/api/bom` | 改為批次查詢／記憶體組裝，避免「主件數 × 子查詢」的 N+1 模式 |
| 前端看板 | `getItems(undefined, { summary: true })` 減少輪詢資料量 |
| 出入庫紀錄頁 | 分頁載入、搜尋 debounce、匯出改走單次大量撈取（上限與後端一致） |
| 庫存報表頁 | `useMemo` + `Map` 彙總，降低 O(n²)；`fetchReport` 以 `useCallback` 穩定宣告順序 |
| 原生模組 | 更換 Node 主版本後須重新編譯 `better-sqlite3`（見第六節） |

---

## 二、後端（`server/server.js`）

### 2.1 資料庫初始化

- **移除**啟動時重複執行的第二次 `db.exec(createTables)`（減少不必要 I/O）。

### 2.2 索引（啟動時 `CREATE INDEX IF NOT EXISTS`）

- `inventory(item_id)`、`inventory(location_id)`  
- `bom_items(main_barcode)`  
- `transactions(timestamp)`、`transactions(item_id)`  

實際是否需要額外複合索引，可再依慢查詢以 `EXPLAIN QUERY PLAN` 調整。

### 2.3 `GET /api/items`

- 查詢參數：**`summary=1` 或 `summary=true`**  
- 行為：僅聚合 `total_quantity`，**不組** `GROUP_CONCAT(... locations ...)`。  
- **庫存查詢頁**等需「儲位分佈」者，請**不要**帶 `summary`（維持原有欄位）。

### 2.4 `GET /api/transactions`

- **回應格式**：`{ items, total, limit, offset }`（**不再**為純陣列）。  
- **參數**：  
  - `limit`：每頁筆數；未傳或無效時預設 **100**；**上限 25000**。  
  - `offset`：位移。  
  - `q`：可選；於伺服端以 `LIKE` 篩選條碼、品名、儲位、經手人／刪除者相關欄位。  

外部腳本若仍假設回傳為陣列，需改為讀取 **`items`**。

### 2.5 `GET /api/bom`

- 改為：一次（或固定少次）查 BOM 明細，再批次查庫存總量與儲位字串，於 Node 端組回與舊版相同結構之主件／元件列表。

---

## 三、前端 API 封裝（`client/src/api.js`）

```text
getItems(q, opts?)
  - opts.summary === true 時會帶 summary=1（適用看板全量料件撈取）。
  - q 僅在「非空字串」時送出，避免多餘 query。
```

---

## 四、前端頁面行為變更

| 檔案 | 變更要點 |
|------|-----------|
| `Dashboard.jsx` | 與儲位並行請求改為 `getItems(undefined, { summary: true })` |
| `TransactionHistory.jsx` | 列表依 `limit`/`offset`/`q` 向後端要資料；搜尋約 300ms debounce；匯出使用 `limit=25000` |
| `Reports.jsx` | 料件／儲位彙總改 `Map` + `useMemo`；`fetchReport` 為 `useCallback` 並列入 `useEffect` 依賴 |
| `Inventory.jsx` | 仍使用 `getItems(search)` **不帶 summary**，儲位字串行為不變 |

---

## 五、部署／驗證建議

1. 重新編譯並佈署前端 `client`（`npm run build`），後端重啟以使索引與路由生效。  
2. 開發者工具 Network 可確認：  
   - 看板：`/api/items?summary=1`  
   - 出入庫紀錄：`/api/transactions?limit=100&offset=0`（及可選 `q`）  
3. 若交易筆數極大：匯出僅能取得 **單次請求上限內**（目前 25000）之篩選結果；超量需另行設計專用匯出或分批 API。

---

## 六、環境注意：`better-sqlite3` 與 Node 版本

更換 **Node.js 主版本**（例如由 20 改 22）後，若出現 `NODE_MODULE_VERSION` / `ERR_DLOPEN_FAILED`：

- 於 **`server` 目錄**執行：`npm rebuild better-sqlite3`（或 `npm rebuild`）。  

此為原生模組 ABI 問題，與本次 API 邏輯無關，但每次升級 Node 均建議重編譯並起機自測。

---

## 七、本地開發：Vite Proxy 與「資料不見」

開發時若僅執行 `client` 的 `npm run dev` 而**未**啟動 `server`，瀏覽器經代理請求 `http://127.0.0.1:3000` 會出現 **`ECONNREFUSED`**，畫面會像沒有資料（實際為**連不到 API**，並非資料庫被清空）。

- **建議**：在專案**根目錄**執行一次 `npm install` 後，使用 **`npm run dev`** 以 `concurrently` **同時**啟動後端（`server` 的 `npm start`）與前端（`client` 的 `npm run dev`）。  
- **或**分兩個終端：先 `cd server && npm start`，再 `cd client && npm run dev`。  
- 前端已加入 **後端連線提示條**（API 無回應時顯示上述說明）。  
- 正式資料檔位置仍為 **`server/warehouse.db`**；備份見 **`server/backups/`**。  
- **`run_locally.bat`**：啟動前會比對 **`server/.node_abi_cache`** 是否與目前 `node -p process.version` 相符；不符時自動執行 **`npm rebuild better-sqlite3`**，避免換過 Node 主版本後出現 `NODE_MODULE_VERSION` / `ERR_DLOPEN_FAILED` 以致後端起不來（Vite Proxy `ECONNREFUSED`）。控制台提示為**中英對照**，便於對照錯誤訊息。

---

## 八、效能項目核對檢查清單（已於程式落地）

可作為發版／驗收時對照；若下列項皆存在，代表本次規劃中的效能強化均已寫入程式（未含：日後可加做的 route code-split、`/api/locations` 輕量化等延伸項）。

| 項目 | 位置／驗證方式 |
|------|----------------|
| DB 輔助索引 | `server/server.js` 啟動時 `CREATE INDEX IF NOT EXISTS`：`inventory(item_id)`、`inventory(location_id)`、`bom_items(main_barcode)`、`transactions(timestamp/item_id)` |
| 料件輕量查詢（看板） | `GET /api/items?summary=1`；`Dashboard.jsx` 使用 `getItems(undefined, { summary: true })`；庫存查詢頁仍為**不帶** `summary` 以保留儲位字串 |
| 交易紀錄分頁／搜尋 | `GET /api/transactions` 回傳 `{ items, total, ... }`，`limit`/`offset`/`q`；`TransactionHistory.jsx` |
| BOM 批次化 | `GET /api/bom`：單一查詢 BOM 列＋批次庫存，非每主件一輪子查詢 |
| 報表彙總複雜度 | `Reports.jsx`：`useMemo` + `Map` 聚合料件／儲位（避免 O(n²) `.find`） |
| 重複 DDL | 已移除第二次 `db.exec(createTables)`（僅保留一次初始化） |

**尚未實作、仍屬後續可選優化**（先前審閱曾建議）：例如「看板與地圖」共用快取以減少雙重輪詢、`/api/locations` 拆輕量版減 JSON 體積、路由級 `React.lazy` 縮小首包等——與您目前「各功能正常」並不衝突，可視資料量再排期。

---

## 九、文件版本

- **檔名**：`1150512_效能提升重大修正說明.md`  
- **對應約定日期**：1150512  
- **內容範圍**：本次效能相關修改與第八節核對檢查清單、第七節本地開發說明；後端離線 Banner 為中英對照文案（`client/src/backendOfflineMessages.js`）。  

（若日後再增修效能項目，建議於此檔增訂「修訂紀錄」小節或另開新編號文件。）
