# 1150513｜佈署失敗原因及解決方式

本文檔記錄本次於 Linux 伺服器以 Docker Compose production 組態部署時，發生 **`http://主機IP:3000/` 無法連線** 之排查結果、根因與處置方式，供日後維運與上線檢核使用。

---

## 一、現象

- 於伺服器目錄執行：`docker compose -f docker-compose.prod.yml up -d --build`，建置顯示成功。
- 自瀏覽器或他機存取 **`http://172.17.88.201:3000/`** 無法連線（或連線被中斷）。
- 伺服器本上執行：

  ```bash
  docker ps -a --filter name=wms-production
  curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:3000/
  ```

  - 容器狀態為 **`Restarting (1)`**（或頻繁重啟）。
  - `curl` 回應為 **`000`**（表示未取得有效 HTTP 回應）。

---

## 二、根本原因

### 2.1 直接原因（本次實際錯誤）

`server/server.js` 於啟動時載入：

```js
const { bi, userFacingCatch } = require('./utils/apiErrors');
```

但部署至伺服器的檔案中，**缺少** `server/utils/apiErrors.js`（僅更新了 `server.js` 或未同步完整 `utils` 目錄）。

因此 Node.js 於容器內拋錯：

```text
Error: Cannot find module './utils/apiErrors'
```

程式無法進入監聽埠號階段，Docker 便以非零結束碼退出，並依 `restart` 策略**反覆重啟**，對外等同服務離線，`curl` 即為 `000`。

### 2.2 為何「本機已有檔案」容器內仍可能沒有

Dockerfile 在建置時以 **`COPY server/ ./server/`** 將**建置當下**主機上的 `server` 目錄拷入映像。若：

- `apiErrors.js` 未出現在伺服器路徑 `/root/wms_system/server/utils/`；或  
- 補檔後**未重新** `docker compose ... up --build`，

則執行中的映像仍可能不含該檔案，問題會持續。

### 2.3 其他曾討論、但未列為本次主因之項目（日後仍可排查）

| 項目 | 說明 |
|------|------|
| `warehouse.db` 掛載成「目錄」 | Compose 將 `./server/warehouse.db` bind mount 到容器；若主機上無此檔案，Docker 可能建立同名**資料夾**，導致 SQLite 無法開啟。**本次 log 並未顯示此情形**，但若日後見資料庫相關啟動錯誤，應優先檢查 `ls -la server/warehouse.db` 類型為檔案而非目錄。 |
| 防火牆 | 容器 `Up` 且本機 `curl 127.0.0.1:3000` 正常，但他機無法連線時，請檢查主機是否放行 **3000/tcp**。 |
| Compose `version` 警告 | 新版 Docker Compose 會提示 `version` 欄位已過時；專案中已移除該欄位僅為消除警告，與可否連線無直接關係。 |

---

## 三、解決方式（已驗證）

### 步驟 1：確認主機上有 `apiErrors.js`

```bash
ls -la /root/wms_system/server/utils/apiErrors.js
```

若不存在，請自開發機上傳（路徑需與專案一致）。範例（Windows PowerShell，`scp`）：

```powershell
scp "<本機專案>\server\utils\apiErrors.js" root@172.17.88.201:/root/wms_system/server/utils/apiErrors.js
```

若目錄尚未建立：

```bash
mkdir -p /root/wms_system/server/utils
```

### 步驟 2：重新建置映像並啟動

```bash
cd /root/wms_system
docker compose -f docker-compose.prod.yml up -d --build
```

### 步驟 3：驗證

```bash
docker ps --filter name=wms-production
docker logs wms-production --tail 40
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:3000/
```

**預期：**

- 容器 **STATUS** 為 **`Up`**（非 `Restarting`）。
- Log 含 **`Database initialized.`** 與 **`Server running on http://localhost:3000`**（或類似監聽訊息）。
- **`curl` 為 `200`**（或至少有正常 HTTP 狀態碼，而非 `000`）。

---

## 四、預防與建議

1. **後端異動請同步整個 `server/`（或至少與 Dockerfile `COPY server/` 範圍一致）**，避免新增 `require('./utils/...')` 卻漏傳對應檔案。
2. 程式碼變更後務必 **`--build`**，讓新建置把更新後的原始檔打進映像（僅在主機新增檔案而不重建時，容器內仍為舊內容）。
3. 上線 checklist 可加一項：**映像建置後**於容器或建置 log 確認無 `MODULE_NOT_FOUND`。
4. 若日後將 `warehouse.db` 相關錯誤寫進啟動流程，請一併閱讀 log 中之中文提示（見 `server/server.js` 對「`warehouse.db` 為目錄」之說明）。

---

## 五、摘要

| 項目 | 內容 |
|------|------|
| **現象** | 佈署後 `:3000` 無法連線；容器 `Restarting`；`curl` 為 `000`。 |
| **根因** | 缺少 **`server/utils/apiErrors.js`**，導致 `require('./utils/apiErrors')` 失敗，程序退出。 |
| **處置** | 補齊 `apiErrors.js` 後執行 **`docker compose -f docker-compose.prod.yml up -d --build`**。 |
| **驗證** | 容器 **`Up`**、log 正常、`curl localhost:3000` 為 **`200`**。 |
