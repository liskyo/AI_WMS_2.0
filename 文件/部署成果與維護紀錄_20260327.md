# 智慧倉儲動態管理系統 2.0 部署完畢回顧 (2026/03/27)

系統已成功部署至正式伺服器 (`172.17.88.201`)，並完成了關鍵 Bug 修復與環境優化。

## 📍 系統存取資訊
* **正式網址**: `http://172.17.88.201:3000`
* **作業系統**: Rocky Linux 10.1
* **技術棧**: React 19 + Node.js + Docker + SQLite

## 🛠️ 已修復與實施的項目
1. **React 19 生產環境打包**:
   - 解決了在生產環境中 `React is not defined` 的報錯，確保 JSX 自動轉換機制一致。
2. **UI 樣式復原**:
   - 補齊了 `tailwind.config.js` 與 `postcss.config.js`，讓深色 UI 正常顯示。
3. **多樓層報表優化**:
   - 修正了「儲位總表」匯出 Excel 的邏輯。現在支援 **全樓層資料自動切分分頁 (Excel Sheets)**。
4. **Git 版本控制管理**:
   - 在伺服器端實施 Git 版控，未來更新僅需 `git pull` 即可，並強化了 `.gitignore` 以保護生產資料庫。
5. **資料庫備份機制**:
   - 設定了 Docker 內的自動備份邏輯，每日凌晨 2 點自動存檔並保留 7 天紀錄。

## 🔄 未來更新維護流程 (SOP)
1. **本機開發**: 在本機電腦修改程式碼。
2. **同步代碼**: `git push` 到 GitHub。
3. **伺服器更新**:
   ```bash
   ssh root@172.17.88.201
   cd ~/wms_system
   git pull origin main
   docker compose -f docker-compose.prod.yml up -d --build
   ```

---
*本文件由 Antigravity AI 助理於部署成功後自動生成。*
